module order {

    requires book; // A requires clause expresses that the required module is needed at compile and run time.
    // requires static book;  // A requires static clause expresses a dependency that is optional at run time.
}